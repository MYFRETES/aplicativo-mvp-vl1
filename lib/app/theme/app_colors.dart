import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF4B0082);
  static const primaryDark = Color(0xFF1A0033);
  static const accent = Color(0xFFFFD700);
  static const white = Colors.white;
  static const black = Color(0xFF111111);
  static const border = Color(0xFF8000FF);
  static const success = Color(0xFF25D366);
  static const background = Color(0xFFF7F4FB);
}
